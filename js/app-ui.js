/* Dutch Vocabulary Trainer V2.4 — Practice and History UI */
(async function(){
'use strict';
const app=window.DutchTrainer;if(!app?.ready)return;try{await app.ready;}catch(e){console.error('V2.4 core failed to initialize',e);return;}
const importer=window.DutchTrainerImport;const $=id=>document.getElementById(id);const screens=['home','dashboard','packs','settings','practice','complete','history'];const ids={home:'homeScreen',dashboard:'dashboardScreen',packs:'packsScreen',settings:'settingsScreen',practice:'practiceScreen',complete:'completeScreen',history:'historyScreen'};let selectedPackId=localStorage.getItem('v24.selectedPackId')||'all';let answerInProgress=false;
function nav(name){screens.forEach(s=>$(ids[s])?.classList.toggle('active',s===name));if(name==='dashboard')renderDashboard();if(name==='packs')renderPacks();if(name==='history')renderHistory();}
async function words(){return app.vocabulary.getAll();}async function selectedWords(){const ws=await words();return selectedPackId==='all'?ws:ws.filter(w=>String(w.packId||'default')===selectedPackId);}
function stats(ws){const m=ws.map(w=>Number(w.mastery)||0);return{total:ws.length,learned:ws.filter(w=>(Number(w.mastery)||0)>=90).length,weak:ws.filter(w=>(Number(w.mastery)||0)<40).length,due:ws.filter(w=>w.isDue||(w.dueAt&&new Date(w.dueAt)<=new Date())).length,average:ws.length?Math.round(m.reduce((a,b)=>a+b,0)/ws.length):0};}
async function renderDashboard(){const ws=await selectedWords(),all=await words(),s=stats(ws),a=stats(all);const set=(id,v)=>{if($(id))$(id).textContent=v;};set('allWords',a.total);set('allLearned',a.learned);set('allDue',a.due);set('allWeak',a.weak);set('allProgressText',a.average+'%');if($('allProgressFill'))$('allProgressFill').style.width=a.average+'%';set('selectedLearned',s.learned);set('selectedDue',s.due);set('selectedWeak',s.weak);set('selectedNew',ws.filter(w=>(Number(w.mastery)||0)<=0).length);set('selectedProgressText',s.average+'%');if($('selectedProgressFill'))$('selectedProgressFill').style.width=s.average+'%';set('selectedFilterLabel',selectedPackId==='all'?'All Vocabulary':selectedPackId);}
async function renderPacks(){const list=$('packsList'),selector=$('v24PackSelector');if(!list||!selector)return;const ws=await words(),groups=new Map();ws.forEach(w=>{const id=String(w.packId||'default');if(!groups.has(id))groups.set(id,[]);groups.get(id).push(w);});selector.innerHTML='<option value="all">All Vocabulary</option>';groups.forEach((items,id)=>{const o=document.createElement('option');o.value=id;o.textContent=`${id} (${items.length})`;selector.append(o);});if(!groups.has(selectedPackId)&&selectedPackId!=='all')selectedPackId='all';selector.value=selectedPackId;selector.onchange=async()=>{selectedPackId=selector.value;localStorage.setItem('v24.selectedPackId',selectedPackId);await renderPacks();await renderDashboard();};list.innerHTML='';if(!groups.size){list.textContent='No vocabulary packs installed.';return;}groups.forEach((items,id)=>{const card=document.createElement('div');card.className='card pack-card';if(id===selectedPackId)card.classList.add('active');const title=document.createElement('strong');title.textContent=id==='default'?'Default Vocabulary':id;const p=document.createElement('p');p.textContent=`${items.length} words`;const use=document.createElement('button');use.type='button';use.className='secondary';use.textContent=id===selectedPackId?'Selected':'Use Pack';use.disabled=id===selectedPackId;use.onclick=async()=>{selectedPackId=id;localStorage.setItem('v24.selectedPackId',selectedPackId);await renderPacks();await renderDashboard();};card.append(title,p,use);list.append(card);});}
async function importSelectedPack(){const input=$('v24PackFile'),status=$('v24ImportStatus'),button=$('v24ImportPackBtn');if(!input?.files?.[0]){if(status)status.textContent='Choose a JSON Word Pack first.';return;}if(!importer){if(status)status.textContent='Word Pack importer is not loaded.';return;}button?.setAttribute('disabled','disabled');if(status)status.textContent='Importing…';try{const result=await importer.importFile(input.files[0],{selectImportedPack:true});selectedPackId=result.packId;localStorage.setItem('v24.selectedPackId',selectedPackId);if(status)status.textContent=`Imported ${result.added} words into “${result.pack.name}”.`;input.value='';await renderPacks();await renderDashboard();}catch(e){console.error(e);if(status)status.textContent=e?.message||'Import failed.';}finally{button?.removeAttribute('disabled');}}
async function renderHistory(){const area=$('historyContent');if(!area)return;const rows=await app.history.getSessions();area.innerHTML='';if(!rows.length){area.textContent='No practice history yet.';return;}const table=document.createElement('table');table.innerHTML='<thead><tr><th>Date</th><th>Exercise</th><th>Words</th><th>Mastery %</th></tr></thead>';const body=document.createElement('tbody');rows.slice().sort((a,b)=>new Date(b.finishedAt||b.startedAt)-new Date(a.finishedAt||a.startedAt)).slice(0,200).forEach(r=>{const tr=document.createElement('tr');const terms=(Array.isArray(r.results)?r.results:[]).map(x=>x.dutch).filter(Boolean);const wordsText=terms.length?[...new Set(terms)].join(', '):(Array.isArray(r.selectedVocabulary)?[...new Set(r.selectedVocabulary.map(x=>x?.dutch||x?.word).filter(Boolean))].join(', '):'');[new Date(r.finishedAt||r.startedAt).toLocaleString(),r.exerciseType||'',wordsText,r.mastery==null?'—':`${Number(r.mastery)||0}%`].forEach(v=>{const td=document.createElement('td');td.textContent=v;tr.append(td);});body.append(tr);});table.append(body);area.append(table);}
function setPracticeProgress(s){const total=Number(s?.questionCount)||0,index=Number(s?.currentIndex)||0,answered=Number(s?.answerCount)||0;if($('sessionCounter'))$('sessionCounter').textContent=total?`${Math.min(total,index+1)}/${total}`:'0/0';if($('sessionProgressFill'))$('sessionProgressFill').style.width=total?`${Math.round(answered/total*100)}%`:'0%';if($('practiceSubtitle'))$('practiceSubtitle').textContent=total?`${answered} answered · ${s.correctCount||0} correct`:'';}
function showMastery(value){let el=$('practiceMastery');if(!el){el=document.createElement('div');el.id='practiceMastery';el.className='mastery-display';$('questionCard')?.insertBefore(el,$('answerArea'));}el.textContent=`Mastery: ${Number(value)||0}%`;el.classList.remove('hidden');}
async function startPractice(){const ws=await selectedWords();if(!ws.length){alert('No vocabulary available. Import or add vocabulary first.');return;}app.practice.reset?.();const type=$('exerciseType')?.value||$('settingsExerciseType')?.value||'meaning';const raw=$('customQuestionCount')?.value||document.querySelector('.countPreset.active')?.dataset.value||$('settingsQuestionCount')?.value||20;const count=Math.max(1,Math.min(500,Number(raw)||20));const session=await app.practice.start({vocabulary:ws,exerciseType:type,questionCount:Math.min(count,ws.length),mode:'full'});if(!session?.success){alert('Could not start practice.');return;}nav('practice');renderQuestion(session.question);}
function renderQuestion(q){if(!q)return;const e=q.exercise||{};if($('exerciseBadge'))$('exerciseBadge').textContent=e.label||q.type||'Exercise';if($('questionPrompt'))$('questionPrompt').textContent=e.prompt||'Answer the question.';if($('questionContext'))$('questionContext').textContent=e.context||'';showMastery(q.word?.mastery||0);const area=$('answerArea');if(!area)return;area.innerHTML='';if(Array.isArray(e.options)&&e.options.length){e.options.forEach(option=>{const label=document.createElement('label');label.className='practice-option';const input=document.createElement('input');input.type='radio';input.name='v24answer';input.value=String(option);label.append(input,document.createTextNode(' '+option));area.append(label);});}else{const input=document.createElement('input');input.id='answerInput';input.type='text';input.autocomplete='off';input.addEventListener('keydown',ev=>{if(ev.key==='Enter'&&!answerInProgress&&!$('checkBtn')?.classList.contains('hidden'))check();});area.append(input);setTimeout(()=>input.focus(),0);}if($('feedbackArea'))$('feedbackArea').innerHTML='';$('feedbackArea')?.classList.remove('feedback','correct','wrong');$('checkBtn')?.classList.remove('hidden');$('checkBtn')?.removeAttribute('disabled');$('nextBtn')?.classList.add('hidden');$('nextBtn')?.removeAttribute('disabled');setPracticeProgress(app.practice.getState?.()||{});}
function answerValue(){return document.querySelector('#answerArea input[type=radio]:checked')?.value||$('answerInput')?.value||'';}
async function check(){if(answerInProgress)return;const value=answerValue().trim();if(!value)return;answerInProgress=true;$('checkBtn')?.setAttribute('disabled','disabled');try{const result=await app.practice.answer(value),f=result.feedback||{},area=$('feedbackArea');if(area){area.className=`feedback ${f.correct?'correct':'wrong'}`;if(f.correct)area.innerHTML='<strong>✓ Correct</strong>';else if(f.almost)area.innerHTML='<strong>≈ Not quite</strong><div>Close — try to be more precise next time.</div><div>Correct answer: '+(f.expected||result.question?.exercise?.correctAnswer||'')+'</div>';else area.innerHTML='<strong>✗ Incorrect</strong><div>Correct answer: '+(f.expected||result.question?.exercise?.correctAnswer||'')+'</div>';}showMastery(f.mastery??result.mastery??0);$('checkBtn')?.classList.add('hidden');$('nextBtn')?.classList.remove('hidden');$('nextBtn')?.removeAttribute('disabled');document.querySelectorAll('#answerArea input').forEach(i=>i.disabled=true);setPracticeProgress(result.state||app.practice.getState?.()||{});}finally{answerInProgress=false;}}
async function next(){if(answerInProgress)return;$('nextBtn')?.setAttribute('disabled','disabled');try{const result=await app.practice.next();if(result.completed){const s=result.state||{};nav('complete');if($('sessionScore'))$('sessionScore').textContent=`${Number(s.correctCount)||0} / ${Number(s.answerCount)||0} correct — Mastery ${Number(s.session?.mastery??0)||0}%`;return;}renderQuestion(result.question);}catch(e){console.error(e);$('nextBtn')?.removeAttribute('disabled');if($('feedbackArea'))$('feedbackArea').textContent=e?.message||'Could not continue.';}}
function exitPractice(){app.practice.reset?.();if($('feedbackArea')){$('feedbackArea').innerHTML='';$('feedbackArea').className='';}if($('answerArea'))$('answerArea').innerHTML='';if($('practiceMastery'))$('practiceMastery').classList.add('hidden');if($('sessionCounter'))$('sessionCounter').textContent='0/0';if($('sessionProgressFill'))$('sessionProgressFill').style.width='0%';nav('home');}
function bind(){$('dashboardBtn')?.addEventListener('click',()=>nav('dashboard'));$('packsBtn')?.addEventListener('click',()=>nav('packs'));$('historyBtn')?.addEventListener('click',()=>nav('history'));$('settingsBtn')?.addEventListener('click',()=>nav('settings'));document.querySelectorAll('.backBtn').forEach(b=>b.addEventListener('click',()=>nav('home')));$('quickPracticeBtn')?.addEventListener('click',startPractice);$('practiceSetupBtn')?.addEventListener('click',()=>$('practiceModal')?.classList.remove('hidden'));$('closePracticeModal')?.addEventListener('click',()=>$('practiceModal')?.classList.add('hidden'));$('startPracticeBtn')?.addEventListener('click',()=>{$('practiceModal')?.classList.add('hidden');startPractice();});$('checkBtn')?.addEventListener('click',check);$('nextBtn')?.addEventListener('click',next);$('exitPracticeBtn')?.addEventListener('click',exitPractice);$('backDashboardBtn')?.addEventListener('click',()=>{app.practice.reset?.();nav('dashboard');});$('practiceAgainBtn')?.addEventListener('click',startPractice);document.querySelectorAll('.countPreset').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.countPreset').forEach(x=>x.classList.remove('active'));b.classList.add('active');if($('customQuestionCount'))$('customQuestionCount').value='';}));$('v24ImportPackBtn')?.addEventListener('click',importSelectedPack);renderDashboard();}
window.DutchTrainerUI={nav,renderDashboard,renderPacks,renderHistory,startPractice,importSelectedPack};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
})();
