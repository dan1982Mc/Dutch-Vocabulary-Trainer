/* Architecture-A application bootstrap. */
(function(){'use strict';
const APP_VERSION='2.0.0';const AppState={version:APP_VERSION,initialized:false,initializing:false,currentView:'home',previousView:null,practiceSession:null,vocabularySelection:null,statistics:null,lastError:null};
function dispatch(name,detail={}){window.dispatchEvent(new CustomEvent(name,{detail:{...detail,appVersion:APP_VERSION}}))}function fail(e,c){AppState.lastError={context:c,message:e?.message||String(e)};console.error(c,e);dispatch('app-error',{error:AppState.lastError});throw e}
async function initialize(){if(AppState.initialized||AppState.initializing)return AppState;AppState.initializing=true;try{if(typeof initDatabase==='function')await initDatabase();if(typeof initializeStorage==='function')await initializeStorage();if(typeof runMigrations==='function')await runMigrations();await window.DutchTrainerPacks.ensureDefaultPack();AppState.vocabularySelection=getVocabularySelection();if(typeof recalculateAllMasteryStates==='function')await recalculateAllMasteryStates();if(typeof initializeScheduler==='function')await initializeScheduler();if(typeof initializeImport==='function')await initializeImport();if(typeof initializeDashboard==='function')await initializeDashboard();if(typeof initializePractice==='function')initializePractice();if(typeof initializeUI==='function')initializeUI();AppState.initialized=true;dispatch('app-ready',{state:AppState});return AppState}catch(e){return fail(e,'Application initialization')}finally{AppState.initializing=false}}
async function vocabulary(){return getAllWords()}async function selectedVocabulary(){return getSelectedVocabulary()}async function changeSelection(source,packId=null){const a={all:selectAllVocabulary,pack:()=>selectVocabularyPack(packId),new:selectNewVocabulary,weak:selectWeakVocabulary,due:selectDueVocabulary};if(a[source])await a[source]();AppState.vocabularySelection=getVocabularySelection();dispatch('app-selection-changed',{selection:AppState.vocabularySelection});return AppState.vocabularySelection}
async function startQuickPractice(options={}){const type=options.exerciseType??await getSetting('practice.exerciseType','meaning');const count=Number(options.questionCount??await getSetting('practice.questionCount',20));const words=await selectedVocabulary();const session=await DutchTrainerPractice.startPractice({mode:'start',exerciseType:type,questionCount:count,vocabulary:words});AppState.practiceSession=session;if(session?.success)navigateTo('practice');return session}
async function startConfiguredPractice(){const type=document.getElementById('exerciseType')?.value||await getSetting('practice.exerciseType','meaning');const custom=document.getElementById('customQuestionCount')?.value;const preset=document.querySelector('.countPreset.active')?.dataset.value;const count=Math.max(1,Number(custom||preset||await getSetting('practice.questionCount',20)));const filter=document.getElementById('vocabularyFilter')?.value||'all';const packId=document.getElementById('packSelector')?.value||'all';await changeSelection(filter,filter==='pack'?packId:null);document.getElementById('practiceModal')?.classList.add('hidden');const session=await DutchTrainerPractice.startPractice({exerciseType:type,questionCount:count,mode:'full'});AppState.practiceSession=session;if(session?.success)navigateTo('practice');return session}
async function checkCurrentAnswer(){const r=document.querySelector('#answerArea input[type=radio]:checked');const input=r||document.querySelector('#answerArea input[type=text],#answerArea textarea,#answerArea select');return DutchTrainerPractice.checkAnswer(input?.value??'')}
function nextCurrentQuestion(){const result=DutchTrainerPractice.nextQuestion();if(result.completed)navigateTo('complete');else if(typeof renderPracticeQuestion==='function')renderPracticeQuestion(result.question);return result}
window.DutchTrainerApp={version:APP_VERSION,state:AppState,initialize,init:initialize,startQuickPractice,startConfiguredPractice,checkCurrentAnswer,nextCurrentQuestion,getVocabulary:vocabulary,getSelectedVocabulary:selectedVocabulary,changeSelection};window.startQuickPractice=startQuickPractice;window.startConfiguredPractice=startConfiguredPractice;window.checkCurrentAnswer=checkCurrentAnswer;window.goToNextPracticeQuestion=nextCurrentQuestion;window.addEventListener('DOMContentLoaded',()=>initialize().catch(()=>{}));
})();